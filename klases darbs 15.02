#include <iostream>;
using namespace std;
int main() {
	int i;
	int x;
	int y;
	cout << "enter 3 numbers\n";
	cin >> i >> x >> y;
	if (i < y && i < x)
		cout << "min is " << i;
	else if (x < i && x < y) {

		cout << "min is " << x;
	}
	else if (y < x && y < i) {

		cout << "min is " << y;
	}
	if (i > y && i > x) {
		cout << "max is " << i;
	}
	else if (y > i && y > x) {
		cout << "max is " << y;
	}
	else if (x > y && x > i) {
		cout << "max is " << x;
	}
}
void pirmuzd1() {
	int i = 1;
	do {
		cout << i * 3 << endl;



		i++;



	} while (i < 126);



}
void tresaisuzd() {
	string i;
	while (i != "stop") {

		cout << i;
		cin >> i;
	}

}
void ceturtaisuuzd(){
	int x = 1;
	string i;
	string i1 = "papaka";
	do {
		cout << "enter pass";
		cin >> i;
		if (i == i1) {
			cout << "pass is correct";
			//return 0;
		}
		x++;
	} while (x < 6);
	cout << "out of attempts";
}
void piektaisuzd() {
	int i = 1000;
	while (i >= 0) {
		cout << i << endl;
		i = i - 5;
	}
}
void otraisuzd() {
	int i;
	cin >> i;
	if (i <= 12) {
		cout << i << "AM" << endl;

	}
	if (i > 12) {
		cout << i - 12 << "PM";
	}
}
